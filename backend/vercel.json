{
    "version": 2,
    "build": {
        "src": "./server.js",
        "use": "@vercel/nodemon",
        "buildCommand": "next build",
        "cleanUrls": true
    },
    "routes": [
        { 
            "src": "/(.*)", 
            "dest": "/server.js" 
        }
    ]
}
